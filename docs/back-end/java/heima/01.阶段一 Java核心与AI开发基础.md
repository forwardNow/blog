<!--#region
@author 吴钦飞
@email wuqinfei@qq.com
@create date 2026-03-07 19:32:53
@modify date 2026-03-10 20:14:13
@desc [description]
#endregion-->

# 阶段一 Java 核心与AI开发基础

## 1.第一章 开发环境搭建 

### 1-2 开发环境搭建（初识Java）

#### 01 阶段导学视频

略

#### 02 Java背景介绍

Java 是由 James Gosling 于 1995 年在 Sun 公司开发的计算机高级编程语言，在 2009 年被 Oracle 公司收购。


Java是什么？

* 是一门高级编程语言。

Java是哪家公司研发的，现在属于哪家公司？

* sun公司、Oracle公司。

Java之父是谁啊？

* 詹姆斯.高斯林

Java能做什么？

* 基本上什么都可以干，主要做企业服务端开发。

Java有哪三大使用平台 ?

* JavaSE（标准版）、JavaEE（企业版）、JavaME（小型版）

#### 03 JDK的下载和安装

开发 Java 程序必须先安装好JDK（Java Development Kit），也就是 Java 开发工具包。

LTS版本 (Long-Term-Support)

* 长期支持版本: 7 、8 、 11 、 17 、 21 、 25
* 非长期版本 一般支持 6 个月

在 Oracle 的官网可以找到JDK的下载链接：[Java Downloads | Oracle 中国](https://www.oracle.com/cn/java/technologies/downloads/)

* jdk-21_windows-x64_bin.exe

查看版本:

```sh
java -version
# java version "21.0.10" 2026-01-20 LTS

javac -version
# javac 21.0.10
```

JDK 的组成

* JVM（Java Virtual Machine）：Java 虚拟机, 真正运行Java程序的地方。
* 核心类库：Java自己写好的程序，给程序员自己的程序调用的。
* JRE（Java Runtime Environment）: Java的运行环境。（JRE = JVM + 核心类库）
* JDK（Java Development Kit）: Java开发工具包。（JDK = JRE + 开发工具）

**小结**

要使用Java，必须先安装什么？去哪里下载？

* JDK （Java Development Kit） 开发者工具包;  Oracle官网。

LTS版本有哪些？很多企业还在使用哪个JDK版本？

* JDK 8、11、17  21 ； 很多企业还在使用JDK 8 / JDK 11。

如何验证JDK是否安装成功了？

* 打开命令行窗口，输入 `java –version`、`javac –version` 看版本号。

JDK中最重要的2个命令程序是什么？各自的作用是啥？

* javac  java
* 编译工具，执行工具。
* javac.exe 、java.exe 是以 exe 扩展名，执行时可以省略 .exe

#### 04 常用DOS命令

win + E : 打开文件资源管理器

win + R : 打开运行


#### 05 开发Java程序的步骤 

HelloWorld.java:

```java
public class HelloWorld {
  public static void main(String[] args) {
    System.out.println("Hello World!");
  }
}
```

```sh
javac HelloWorld.java

java HelloWorld
```

#### 06 HelloWorld代码详解和常见错误

开发一个Java程序要经历哪三个步骤?

* 编写、编译(javac)、运行(java)

Java代码编写有什么基本要求?

* 文件名称的后缀必须是 java 结尾
* 文件名称必须与代码的类名称一致
* 必须使用英文模式下的符号

#### 07 Java程序执行原理

Java 程序的执行原理是什么样的?

* 不管是什么样的高级编程语言
* 最终要翻译成计算机底层可以识别的机器语言

机器语言是由什么组成的?

* 0 和 1

Java 跨平台指的是什么？原理又是什么？

* Java 程序可以在任意操作系统中运行
* 在不同的操作系统中，安装与之对应版本的 JVM 虚拟机

### 1-3 开发环境搭建（环境变量配置及开发工具使用）

#### 01 Path环境变量

Path 环境变量用于记住程序路径，方便在命令行窗口的任意目录启动程序

修改完，要重启 CMD

#### 02 配置JDK到Path环境变量

安装 JDK 时，会将 `C:\Program Files (x86)\Common Files\Oracle\Java\javapath` 写入 path 环境变量

该路径是个阉割版，只有几个工具: （应该将其删掉）

* java.exe
* javaw.exe
* javaws.exe

JAVA_HOME：告诉操作系统 JDK 安装在了哪个位置（将来其他技术要通过这个环境变量找JDK）

**小结**

什么是Path环境变量？

* 用于配置程序的路径，方便我们在命令行窗口的任意目录下启动该程序。

JDK安装时，关于环境变量的配置，需要注意什么？

* 较新版本的JDK会自动配置PATH环境变量，较老的JDK版本则不会。
* 建议还是自己配置一下“PATH”、 “JAVA_HOME”

#### 03 IDEA介绍下载和安装

IDE 集成环境：把代码编写，编译，执行，调试等多种功能综合到一起的开发工具

#### 04 IDEA中的第一个代码

IDEA中的层级结构是?

* project、module、package、class

Project、Module、Package 本质来讲是什么？

* 文件夹，管理类文件的

生成 main 方法的快捷键是？

* psvm、main

生产打印数据的快捷键是？

* sout

#### 05 IDEA中类-模块-项目的操作

修改项目名称:

1. 项目结构 -> 项目 -> Name: 设置名称
2. 关闭项目，删除最近打开的条目
3. 文件资源管理器: 给项目目录重命名
4. 打开项目，右键项目，重构，重命名

## 2.第二章 基础语法

### 01 注释

注释是什么？

* 写在程序中对程序进行解释说明的文字

Java程序中书写注释的方式有几种，各自有什么不同？

* 单行注释：//
* 多行注释：/*  */
* 文档注释：/**  */

注释有什么特点？为什么？

* 不影响程序的执行，编译后的 class 文件中已经没有注释了。

### 02 关键字和字面量

### 03 变量的介绍和定义

### 04 变量的注意事项

### 05 标识符

### 06 数据类型

### 07 数据类型细节补充

### 08 算数运算符和数值拆分案例

### 09 字符串拼接操作

### 10 自增自减运算符

### 11 类型转换

### 12 类型转换面试题

### 13 赋值运算符和关系运算符

### 14、逻辑运算符

### 15 三元运算符和运算符优先级

### 16 Scanner键盘录入

### 17 package管理类文件

### 18 方法介绍

### 19 方法基本的定义和调用格式

### 20 方法的调用流程

### 21 带参数的方法

### 22 带返回值的方法

### 23 方法通用定义格式和练习

### 24 方法注意事项和重载

### 25 IDEA安装AI插件
