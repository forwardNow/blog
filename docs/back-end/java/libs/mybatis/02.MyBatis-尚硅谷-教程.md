<!--#region
@author 吴钦飞
@email wuqinfei@qq.com
@create date 2026-04-04 11:51:41
@modify date 2026-04-20 18:56:05
@desc [description]
#endregion-->

# MyBatis-尚硅谷-教程

视频: [MyBatis零基础教程，mybatis快速上手，mybatis入门到项目实战](https://www.bilibili.com/video/BV1VP4y1c7j7)

项目: heima/MyBatisDemo

计划:

| 索引 | 名称 | 时长 | 计划完成日期 | 实际完成日期 | 说明 |
| -------- | ---- | -------- | --------- | ----------- | ---- |
| 1 | 01_MyBatis课程介绍 | 03:24 | 2026-04-04 |  2026-04-04 11:54  |  √  |
| 2 | 02_MyBatis的历史 | 04:13 | 2026-04-04 |  2026-04-04 12:14  |  √  |
| 3 | 03_MyBatis的特性 | 08:52 | 2026-04-04 |  2026-04-04 12:23  |  √  |
| 4 | 04_MyBatis的下载 | 02:11 | 2026-04-05 |  2026-04-04 12:39  |  √  |
| 5 | 05_MyBatis和其他持久化层技术的对比 | 05:37 | 2026-04-05 |  2026-04-04 12:47  |  √  |
| 6 | 06_搭建MyBatis框架之开发环境 | 05:01 | 2026-04-05 |  2026-04-05 21:42  |  √  |
| 7 | 07_搭建MyBatis框架之创建maven工程 | 02:38 | 2026-04-06 |  2026-04-07 12:19  |  √  |
| 8 | 08_搭建MyBatis框架之创建MyBatis的核心配置文件 | 09:22 | 2026-04-06 |  2026-04-07 17:32  |  √  |
| 9 | 09_搭建MyBatis框架之创建mapper接口 | 05:57 | 2026-04-06 |  2026-04-07 18:22  |  √  |
| 10 | 10_搭建MyBatis框架之创建MyBatis的映射文件 | 14:24 | 2026-04-07 |  2026-04-09 18:34  |  √  |
| 11 | 11_搭建MyBatis框架之测试添加功能 | 13:00 | 2026-04-07 |  2026-04-10 18:18  |  √  |
| 12 | 12_搭建MyBatis框架之优化功能 | 07:24 | 2026-04-07 |  2026-04-10 18:34  |  √  |
| 13 | 13_搭建MyBatis框架之测试修改和删除功能 | 07:09 | 2026-04-08 |  2026-04-10 22:31  |  √  |
| 14 | 14_搭建MyBatis框架之测试查询功能 | 13:31 | 2026-04-08 |  2026-04-11 10:42  |  √  |
| 15 | 15_MyBatis核心配置文件之environment | 11:08 | 2026-04-08 |  2026-04-11 11:23  |  √  |
| 16 | 16_MyBatis核心配置文件之properties | 07:12 | 2026-04-09 |  2026-04-11 11:38  |  √  |
| 17 | 17_MyBatis核心配置文件之typeAliases | 11:37 | 2026-04-09 |  2026-04-11 12:28  |  √  |
| 18 | 18_MyBatis核心配置文件之mappers | 07:43 | 2026-04-09 |  2026-04-11 12:47  |  √  |
| 19 | 19_思考：映射文件中的SQL该如何拼接 | 05:52 | 2026-04-10 |  2026-04-11 13:53  |  √  |
| 20 | 20_在idea中设置核心配置文件的模板 | 13:08 | 2026-04-10 |  2026-04-11 16:08  |  √  |
| 21 | 21_在idea中设置映射文件的模板 | 03:42 | 2026-04-10 |  2026-04-11 16:28  |  √  |
| 22 | 22_封装SqlSessionUtils工具类并测试功能 | 08:09 | 2026-04-11 |  2026-04-11 17:05  |  √  |
| 23 | 23_MyBatis获取参数值的两种方式#{}和${} | 08:18 | 2026-04-11 |  2026-04-11 18:20  |  √  |
| 24 | 24_MyBatis获取参数值的各种情况（1） | 09:32 | 2026-04-11 |  2026-04-11 18:35  |  √  |
| 25 | 25_MyBatis获取参数值的各种情况（2） | 11:16 | 2026-04-12 |  2026-04-11 18:51  |  √  |
| 26 | 26_MyBatis获取参数值的各种情况（3） | 06:49 | 2026-04-12 |  2026-04-11 21:07  |  √  |
| 27 | 27_MyBatis获取参数值的各种情况（4） | 08:25 | 2026-04-12 |  2026-04-11 21:20  |  √  |
| 28 | 28_MyBatis获取参数值的各种情况（5） | 11:51 | 2026-04-13 |  2026-04-11 21:35  |  √  |
| 29 | 29_@Param源码解析 | 22:41 | 2026-04-13 |  2026-04-12 09:59  |  √  |
| 30 | 30_MyBatis各种查询功能（1） | 13:11 | 2026-04-13 |  2026-04-12 10:42  |  √  |
| 31 | 31_MyBatis各种查询功能（2） | 07:48 | 2026-04-14 |  2026-04-12 10:55  |  √  |
| 32 | 32_MyBatis各种查询功能（3） | 06:20 | 2026-04-14 |  2026-04-12 11:10  |  √  |
| 33 | 33_MyBatis各种查询功能（4） | 08:32 | 2026-04-14 |  2026-04-12 12:01  |  √  |
| 34 | 34_MyBatis处理模糊查询 | 09:37 | 2026-04-15 |  2026-04-13 12:21  |  √  |
| 35 | 35_MyBatis处理批量删除 | 06:28 | 2026-04-15 |  2026-04-13 12:39  |  √  |
| 36 | 36_MyBatis处理动态设置表名 | 05:09 | 2026-04-15 |  2026-04-13 18:04  |  √  |
| 37 | 37_MyBatis获取添加功能自增的主键 | 13:17 | 2026-04-16 |  2026-04-13 18:54  |  √  |
| 38 | 38_搭建MyBatis框架 | 11:40 | 2026-04-16 |  2026-04-14 12:48  |  √  |
| 39 | 39_通过字段别名解决字段名和属性名的映射关系 | 06:28 | 2026-04-16 |  2026-04-14 18:30  |  √  |
| 40 | 40_通过全局配置mapUnderscoreToCamelCase解决字段名和属性名的映射关系 | 07:00 | 2026-04-17 |  2026-04-14 18:41  |  √  |
| 41 | 41_通过resultMap解决字段名和属性名的映射关系 | 10:59 | 2026-04-17 |  2026-04-14 19:00  |  √  |
| 42 | 42_通过级联属性赋值解决多对一的映射关系 | 11:48 | 2026-04-17 |  2026-04-15 12:17  |  √  |
| 43 | 43_通过association解决多对一的映射关系 | 05:19 | 2026-04-18 |  2026-04-15 12:28  |  √  |
| 44 | 44_通过分步查询解决多对一的映射关系 | 16:24 | 2026-04-18 |  2026-04-15 18:51  |  √  |
| 45 | 45_延迟加载 | 11:16 | 2026-04-18 |  2026-04-15 19:12  |  √  |
| 46 | 46_通过collection解决一对多的映射关系 | 11:19 | 2026-04-19 |  2026-04-15 21:04  |  √  |
| 47 | 47_通过分步查询解决一对多的映射关系 | 12:07 | 2026-04-19 |  2026-04-16 09:01  |  √  |
| 48 | 48_动态SQL简介 | 07:08 | 2026-04-19 |  2026-04-16 09:11  |  √  |
| 49 | 49_动态SQL之if标签 | 13:57 | 2026-04-20 |  2026-04-17 12:36  |  √  |
| 50 | 50_动态SQL之where标签 | 08:22 | 2026-04-20 |  2026-04-17 17:09  |  √  |
| 51 | 51_动态SQL之trim标签 | 06:20 | 2026-04-20 |  2026-04-17 17:24  |  √  |
| 52 | 52_动态SQL之choose、when、otherwise | 10:40 | 2026-04-21 |  2026-04-17 18:14  |  √  |
| 53 | 53_动态SQL之foreach标签（1） | 14:34 | 2026-04-21 |  2026-04-18 10:40  |  √  |
| 54 | 54_动态SQL之foreach标签（2） | 11:02 | 2026-04-21 |  2026-04-18 11:02  |  √  |
| 55 | 55_动态SQL之sql标签 | 04:20 | 2026-04-22 |  2026-04-18 11:16  |  √  |
| 56 | 56_MyBatis的一级缓存 | 08:43 | 2026-04-22 |  2026-04-18 19:49  |  √  |
| 57 | 57_使一级缓存失效的四种情况 | 07:07 | 2026-04-22 |  2026-04-18 20:24  |  √  |
| 58 | 58_MyBatis的二级缓存 | 12:07 | 2026-04-23 |  2026-04-20 16:58  |  √  |
| 59 | 59_MyBatis二级缓存相关配置 | 06:58 | 2026-04-23 |  2026-04-20 17:13  |  √  |
| 60 | 60_MyBatis缓存查询的顺序 | 02:50 | 2026-04-23 |  2026-04-20 17:16  |  √  |
| 61 | 61_MyBatis整合第三方缓存EHCache | 09:44 | 2026-04-24 |  2026-04-20 18:15  |  √  |
| 62 | 62_MyBatis逆向工程之清晰简洁版 | 13:16 | 2026-04-24 |  2026-04-20 18:29  |  √  |
| 63 | 63_MyBatis逆向工程之奢华尊享版（1） | 18:43 | 2026-04-24 |  2026-04-20 18:55  |  √  |
| 64 | 64_MyBatis逆向工程之奢华尊享版（2） | 04:28 | 2026-04-25 |  2026-04-20 18:55  |  √  |
| 65 | 65_MyBatis的分页插件简介 | 09:25 | 2026-04-25 |  -  |  -  |
| 66 | 66_MyBatis分页插件的使用 | 16:01 | 2026-04-25 |  -  |  -  |

## 1. 简介

### 1.1. 其他持久化层技术的对比

**JDBC**

* SQL 夹杂在Java代码中耦合度高，导致硬编码内伤
* 维护不易且实际开发需求中 SQL 有变化，频繁修改的情况多见
* 代码冗长，开发效率低

**Hibernate 和 JPA**

* 操作简便，开发效率高
* 程序中的长难复杂 SQL 需要绕过框架
* 内部自动生产的 SQL，不容易做特殊优化
* 基于全映射的全自动框架，大量字段的 POJO 进行部分映射时比较困难。
* 反射操作太多，导致数据库性能下降

**MyBatis**

* 轻量级，性能出色
* SQL 和 Java 编码分开，功能边界清晰。Java代码专注业务、SQL语句专注数据
* 开发效率稍逊于HIbernate，但是完全能够接受

## 2. 快速开始

文档: https://mybatis.org/mybatis-3/getting-started.html

### 2.1. 依赖

```xml
<dependency>
    <groupId>org.mybatis</groupId>
    <artifactId>mybatis</artifactId>
    <version>3.5.10</version>
</dependency>
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <version>8.3.0</version>
</dependency>


<dependency>
    <groupId>junit</groupId>
    <artifactId>junit</artifactId>
    <version>4.13.2</version>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.24</version>
</dependency>
```

### 2.2. 核心配置文件

位置: src/main/resources/mybatis-config.xml

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 数据连接信息 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_demo"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>
    <!-- 映射文件 -->
    <mappers>
        <mapper resource="mappers/UserMapper.xml"/>
    </mappers>
</configuration>
```

### 2.3. 建表

```sql
CREATE TABLE `t_user` (
  `id` int NOT NULL AUTO_INCREMENT,
  `username` varchar(20) ,
  `password` varchar(20) ,
  `age` int ,
  `sex` char(1) ,
  `email` varchar(20) ,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB;
```

### 2.4. 实体类

位置: org.example.pojo.User

```java
@Data
@AllArgsConstructor(staticName = "of")
@NoArgsConstructor
public class User {
    private Integer id;
    private String username;
    private String password;
    private Integer age;
    private String sex;
    private String email;
}
```

### 2.5. Mapper 接口

位置: org.example.mapper.UserMapper

```java
// MyBatis 面向接口编程的两个一致：
// 1. 映射文件的 namespace 要和 mapper 接口的全类名一致
// 2. 映射文件中 SQL 语句的 id 要和 mapper 接口方法名一致
public interface UserMapper {
    int insertUser();
}
```

### 2.6. 映射文件

位置: src/main/resources/mappers/UserMapper.xml

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UserMapper">
    <insert id="insertUser">
        insert into t_user values (null, 'admin', '123456', 23, '男', 'admin@qq.com')
    </insert>
</mapper>
```

### 2.7. log4j 日志

依赖:

```xml
<dependency>
    <groupId>log4j</groupId>
    <artifactId>log4j</artifactId>
    <version>1.2.17</version>
</dependency>
```

配置: `src/main/resources/log4j.xml`

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">
<log4j:configuration xmlns:log4j="http://jakarta.apache.org/log4j/">
    <appender name="STDOUT" class="org.apache.log4j.ConsoleAppender">
        <param name="Encoding" value="UTF-8" />
        <layout class="org.apache.log4j.PatternLayout">
            <param name="ConversionPattern" value="%-5p %d{MM-dd HH:mm:ss,SSS}
%m (%F:%L) \n" />
        </layout>
    </appender>
    <logger name="java.sql">
        <level value="debug" />
    </logger>
    <logger name="org.apache.ibatis">
        <level value="info" />
    </logger>
    <root>
        <level value="debug" />
        <appender-ref ref="STDOUT" />
    </root>
</log4j:configuration>
```

### 2.8. 测试

```java
@Test
public void testMyBatis() throws Exception {
    // 加载核心配置文件
    InputStream resource = Resources.getResourceAsStream("mybatis-config.xml");
    // 获取 SqlSessionFactoryBuild
    SqlSessionFactoryBuilder sqlSessionFactoryBuilder = new SqlSessionFactoryBuilder();
    // 获取 SqlSessionFactory
    SqlSessionFactory sqlSessionFactory = sqlSessionFactoryBuilder.build(resource);
    // 获取 SqlSession (默认不自动提交事务，可以通过传入 true 来设置自动提交事务)
    SqlSession sqlSession = sqlSessionFactory.openSession(true);
    // 获取 mapper 接口对象
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);

    // 添加用户
    int result = userMapper.insertUser();
    System.out.println("result: " + result);

    // 提交事务 (获取 SqlSession 时如果没有设置自动提交事务，则需要手动提交)
    // sqlSession.commit();

    // 关闭会话
    sqlSession.close();
}
```

### 2.9. 查询

```xml
<!--
    必须设置 resultType 或 resultMap
    resultType: 默认的映射关系。表的字段名与实体类的属性名一一对应
    resultMap: 自定义的映射关系。字段名与属性名不一致，一对多
-->
<select id="getUserById" resultType="org.example.pojo.User">
    select * from t_user where id = 2
</select>

<select id="getAllUsers" resultType="org.example.pojo.User">
    select * from t_user
</select>
```

```java
public interface UserMapper {
    User getUserById();
    List<User> getAllUsers();
}
```

```java
@Test
public void testGetUserById() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    User user = userMapper.getUserById();
    System.out.println("user: " + user);
}

@Test
public void testGetAllUsers() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    List<User> users = userMapper.getAllUsers();
    System.out.println("users: " + users);
}
```

## 3. MyBatis 核心配置文件

### 3.1. 数据源设置 - environment

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--
    environments: 配置多个连接数据库的环境
        属性:
            default: 设置默认使用的环境ID
    -->
    <environments default="development">

        <!--
        environment
            属性:
                id: 环境ID
        -->
        <environment id="development">
            <!--
            transactionManager: 配置事务管理器
                属性:
                    type: 事务管理器类型, 值为 "JDBC" | "MANAGED"
                        JDBC: 使用 JDBC 原生的事务管理方式，事务的提交或回滚需要手动处理
                        MANAGED: 使用 JTA (Java Transaction API)，需要 JTA 实现类。比如交给 Spring 管理
            -->
            <transactionManager type="JDBC"/>
            <!--
            dataSource: 配置数据源
                属性:
                    type: 数据源类型, 值为 "POOLED" | "UNPOOLED" | "JNDI"
                        POOLED: 使用数据库连接池
                        UNPOOLED: 不使用数据库连接池
                        JNDI: 使用上下文中的数据源，比如使用 Spring 管理的数据源
            -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_demo"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>

        <environment id="test">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED"></dataSource>
        </environment>

    </environments>
    <!-- 映射文件 -->
    <mappers>
        <mapper resource="mappers/UserMapper.xml"/>
    </mappers>
</configuration>
```

### 3.2. 引入外部配置文件 - properties

src/main/resources/jdbc.properties:

```properties
jdbc.driver=com.mysql.cj.jdbc.Driver
jdbc.url=jdbc:mysql://localhost:3306/mybatis_demo
jdbc.username=root
jdbc.password=123456
```

引入 properties 文件，使用文件中的属性

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--
    properties: 配置外部属性文件
        属性:
            resource: 引用属性文件
    -->
    <properties resource="jdbc.properties" />

    <environments default="development">
        <environment id="development">
            <dataSource type="POOLED">
                <!-- 使用 “ ${属性名} ” 的方式引用属性 -->
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
</configuration>
```

### 3.3. 别名 - typeAliases

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="jdbc.properties" />

    <typeAliases>
        <!--
        typeAlias: 配置单个类型的别名
            属性:
                type: 指定别名对应的类型
                alias: 给类型指定一个别名，默认为类名且不区分大小写
        -->
        <typeAlias type="org.example.pojo.User" alias="User" />
        <!--
        package: 批量给包下的类配置别名
        -->
        <package name="org.example.pojo"/>
    </typeAliases>
    
    <!-- ... -->
</configuration>
```

映射文件:

```xml
<!-- resultType 的值 使用别名 -->
<select id="getAllUsers" resultType="User">
    select * from t_user
</select>
```

### 3.4. 引入映射文件 - mappers

配置:

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- ... -->
    
    <!-- 映射文件 -->
    <mappers>
        <!--
        <mapper resource="mappers/UserMapper.xml"/>
        -->

        <!--
        以包为单位引入映射文件
        要求:
            1. 包名一致: 映射文件所在的包 与 mapper 接口的包 一致
            2. 名称一致: 映射文件的名称与 mapper 接口的名称 一致
        -->
        <package name="org.example.mapper" />
    </mappers>
</configuration>
```

包名一致:

* mapper 接口所在的包: org.example.mapper
* 映射文件所在的目录: src/main/resources/org/example/mapper

名称一致:

* mapper 接口的名称: UserMapper.java
* 映射文件的名称: UserMapper.xml

## 4. MyBatis 获取参数值

### 4.1. 获取参数值的两种方式

两种方式:

* `${}`: 类比 字符串拼接
* `#{}`: 类比 占位符赋值。不需要考虑 SQL 注入，也不需要给 字符串值 拼接单引号

`${}`:

* `"select * from t_user where username = '" + username + "'"`
* `"select * from t_user where username = '${username}'"`

`#{}`:

* `"select * from t_user where username = ?"`
* `"select * from t_user where username = #{username}"`

### 4.2. 单个参数 - 字面量类型

```java
public interface UserMapper {
    User getUserByUsername(String username);
}
```

```xml
<!--  
    这种单个参数，可以通过任意名称获取它的值，但建议使用跟形参名称一致的名称
-->
<select id="getUserByUsername" resultType="User" >
    <!--
    select * from t_user where username = #{username}
    -->
    select * from t_user where username = '${username}'
</select>
```

```java
@Test
public void testGetUserByUsername() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    User user = userMapper.getUserByUsername("admin");
    System.out.println("user: " + user);
}
```

### 4.3. 多个参数

通过 `arg0, arg1, ...` 或 `param1, param2, ...` 来访问

```java
public interface UserMapper {
    User checkLogin(String username, String password);
}
```

```xml
<select id="checkLogin" resultType="User" >
    <!--
    select * from t_user where username = #{arg0} and password = #{arg1}
    -->
    select * from t_user where username = #{param1} and password = #{param2}
</select>
```

```java
@Test
public void testCheckLogin() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    User user = userMapper.checkLogin("admin", "123456");
    System.out.println("user: " + user);
}
```

### 4.4. 单个参数 - Map 类型

单个 Map 类型的参数，在映射文件里可以直接访问 map 里的键

```java
public interface UserMapper {
    User checkLoginByMap(Map<String, Object> map);
}
```

```xml
<select id="checkLoginByMap" resultType="User" >
    select * from t_user where username = #{username} and password = #{password}
</select>
```

```java
@Test
public void testCheckLoginByMap() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);

    Map<String, Object> map = new HashMap<>();
    map.put("username", "admin");
    map.put("password", "123456");
    
    User user = userMapper.checkLoginByMap(map);
    System.out.println("user: " + user);
}
```

### 4.5. 单个参数 - 实体类型

单个 实体类型 的参数，在映射文件里可以直接访问 实体的属性

```java
public interface UserMapper {
    int insertUser(User user);
}
```

```xml
<insert id="insertUser">
    insert into t_user values(null, #{username}, #{password}, #{age}, #{sex}, #{email})
</insert>
```

```java
@Test
public void testInsertUser() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    int result = userMapper.insertUser(User.of(null, "admin3", "123456", 18, "男", "admin3@qq.com"));
    System.out.println("result: " + result);
}
```

### 4.6. 命名参数

使用 `@Param` 指定访问 该参数值 的 键名

可以使用自定义的键名 及 `param1, param2, ...` 访问参数

```java
public interface UserMapper {
    User checkLoginByParam(@Param("username") String username, @Param("password") String password);
}
```

```xml
<select id="checkLoginByParam" resultType="User" >
    select * from t_user where username = #{username} and password = #{password}
</select>
```

```java
@Test
public void testCheckLoginByParam() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    User user = userMapper.checkLoginByParam("admin", "123456");
    System.out.println("user: " + user);
}
```

## 5. 查询

### 5.1. 一条数据 - 返回 `Entity`

如果查询返回的记录数大于 1 ，则会抛异常: TooManyResultsException

```java
User getUserById(@Param("id") Integer id);
```

```xml
<select id="getUserById" resultType="User" >
    select * from t_user where id = #{id}
</select>
```

### 5.2. 多条数据 - 返回 `List<Entity>`

```java
List<User> getAllUser();
```

```xml
<select id="getAllUser" resultType="User" >
    select * from t_user
</select>
```

### 5.3. 单个数据 - 返回 基本类型

返回单行单列的数据

示例:

```java
Integer getCount();
```

```xml
<select id="getCount" resultType="java.lang.Integer">
    <!-- count(*) 和 count(1) 返回的结果是一样的 -->
    select count(*) from t_user
</select>
```

内置类型别名:

| Alias | Mapped Type |
| - | - |
| _byte | byte |
| _char (since 3.5.10) | char |
| _character (since 3.5.10) | char |
| _long | long |
| _short | short |
| _int | int |
| _integer | int |
| _double | double |
| _float | float |
| _boolean | boolean |
| string | String |
| byte | Byte |
| char (since 3.5.10) | Character |
| character (since 3.5.10) | Character |
| long | Long |
| short | Short |
| int | Integer |
| integer | Integer |
| double | Double |
| float | Float |
| boolean | Boolean |
| date | Date |
| decimal | BigDecimal |
| bigdecimal | BigDecimal |
| biginteger | BigInteger |
| object | Object |
| date[] | Date[] |
| decimal[] | BigDecimal[] |
| bigdecimal[] | BigDecimal[] |
| biginteger[] | BigInteger[] |
| object[] | Object[] |
| map | Map |
| hashmap | HashMap |
| list | List |
| arraylist | ArrayList |
| collection | Collection |
| iterator | Iterator |


参考: https://mybatis.org/mybatis-3/configuration.html#typeAliases

### 5.4. 一条数据 - 返回 `Map<String, Object>`

当返回的结果没有实体类接收时，可以返回 map

```java
Map<String, Object> getUserByIdToMap(@Param("id") Integer id);
```

```xml
<select id="getUserByIdToMap" resultType="map" >
    select * from t_user where id = #{id}
</select>
```

### 5.5. 多条数据 - 返回 `List<Map<String, Object>>`

```java
List<Map<String, Object>> getAllUsersToMapList();
```

```xml
<select id="getAllUsersToMapList" resultType="map" >
    select * from t_user
</select>
```

### 5.6. 多条数据 - 返回 `Map<Integer, Entity>`

```java
// 指定哪个字段的值作为 键名
// 类比: list 转 map: key 为 item.id 的值, value 为 item
@MapKey("id")
Map<Integer, Object> getAllUsersToMap();
```

```xml
<select id="getAllUsersToMap" resultType="map" >
    select * from t_user
</select>
```

返回结果:

```text
{
    2={password=123456, sex=男, id=2, age=23, email=admin@qq.com, username=admin}, 
    3={password=123456, sex=男, id=3, age=23, email=admin@qq.com, username=admin2}, 
    4={password=123456, sex=男, id=4, age=18, email=admin3@qq.com, username=admin3}
}
```

## 6. 特殊 SQL

### 6.1. 模糊查询

```java
List<User> getUserByLike(@Param("username") String username);
```

```xml
<select id="getUserByLike" resultType="User">
    <!--
    错误:
        '%#{username}%' 会处理成 `%?%`,
        而 `%?%` 中的 "?" 会被当成普通的字符串 而非占位符,
        导致给占位符赋值时 报错
    -->
    <!--
    select * from t_user where username like '%#{username}%'
    -->

    <!--
    方案 1: 使用 ${} 进行 “字符串拼接”
    -->
    <!--
    select * from t_user where username like '%${username}%'
    -->

    <!--
    方案 2: 使用 concat 进行 字符串拼接
    -->
    <!--
    select * from t_user where username like concat('%', #{username}, '%')
    -->

    <!--
    方案 3: 双引号拼接。【常用】【推荐】
    -->
    select * from t_user where username like "%"#{username}"%"
</select>
```

### 6.2. 批量删除

```java
// ids 的格式为 "1,2,3"
int deleteMany(@Param("ids") String ids);
```

```xml
<delete id="deleteMany">
    <!-- #{} 会自动加单引号，导致生成非预期的 SQL -->
    delete from t_user where id in (${ids})
</delete>
```

### 6.3. 动态设置表名

```java
List<User> getUserByTableName(@Param("tableName") String tableName);
```

```xml
<!-- 直接传数据库的表的名称 即可 -->
<select id="getUserByTableName" resultType="User">
    select * from ${tableName}
</select>
```

### 6.4. 插入时获取自增的主键

```java
int insertUserAndGetId(User user);
```

```xml
<!--
insert 语句的返回值为影响的行数，想要返回 id 则需要通过实体类的属性
    useGeneratedKeys: 使用了自增
    keyProperty: 指定将自增结果保存在哪个属性中
-->
<insert id="insertUserAndGetId" useGeneratedKeys="true" keyProperty="id">
    insert into t_user values(null, #{username}, #{password}, #{age}, #{sex}, #{email})
</insert>
```

```java
@Test
public void testInsertUserAndGetId() {
    UserMapper userMapper = sqlSession.getMapper(UserMapper.class);
    User user = User.of(null, "admin4", "123456", 18, "男", "admin4@qq.com");
    // 插入成功后，user.id 会回填自动生成的自增 id
    userMapper.insertUserAndGetId(user);
    System.out.println("user: " + user);
}
```

## 7. 自定义映射 resultMap

数据库:

```sql
CREATE TABLE `t_emp` (
  `emp_id` int NOT NULL AUTO_INCREMENT,
  `emp_name` varchar(20) ,
  `age` int ,
  `sex` char(1) ,
  `email` varchar(20) ,
  `dept_id` int,
  PRIMARY KEY (`emp_id`)
) ENGINE=InnoDB;

CREATE TABLE `t_dept` (
  `dept_id` int NOT NULL AUTO_INCREMENT,
  `dept_name` varchar(20) ,
  PRIMARY KEY (`dept_id`)
) ENGINE=InnoDB;

insert into t_emp values(1, '张1', 11, '男', '1@qq.com', 1);
insert into t_emp values(2, '张2', 22, '女', '2@qq.com', 2);
insert into t_emp values(3, '张3', 33, '男', '3@qq.com', 3);
insert into t_emp values(4, '张4', 44, '女', '4@qq.com', 1);
insert into t_emp values(5, '张5', 55, '男', '5@qq.com', 2);

insert into t_dept values(1, '部门1');
insert into t_dept values(2, '部门2');
insert into t_dept values(3, '部门3');
```

实体类:

```java
public class Emp {
    private Integer empId;
    private String empName;
    private Integer age;
    private String sex;
    private String email;
}

public class Dept {
    private Integer deptId;
    private String deptName;
}
```

### 7.1. 处理 字段与属性 的映射关系

字段名为 emp_id, 属性名为 empId, 两者不一致，会导致查询出来的实体的属性为空

#### 7.1.1. 方式 1: 字段别名

为字段起别名，保持与属性名一致

```java
List<Emp> getAllEmp();
```

```xml
<select id="getAllEmp" resultType="Emp">
    select
        emp_id as empId,
        emp_name as empName,
        age,
        sex,
        email
    from t_emp
</select>
```

#### 7.1.2. 方式 2: 全局配置-下划线转驼峰

文档: https://mybatis.org/mybatis-3/configuration.html#settings

mybatis-config.xml:

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--
        全局配置
    -->
    <settings>
        <!-- 自动将 下划线字段 映射到 驼峰属性 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
</configuration>
```

接口: 

```java
List<Emp> getAllEmp2();
```

映射文件:

```xml
<select id="getAllEmp2" resultType="Emp">
    select * from t_emp
</select>
```

#### 7.1.3. 方式 3: resultMap

自定义映射关系。一般用于 一对多、多对一的映射

```java
List<Emp> getAllEmp3();
```

```xml
<!--
resultMap: 配置结果集的映射关系
    属性:
        id: 映射结果集的主键字段
        type: 映射结果集的实体类

    子标签:
        id: 映射结果集的主键字段
            属性:
                property: 实体类属性名
                column: SQL语句中查询出来的列名
        result: 映射结果集的普通字段

    注意:
        所有属性都要列出来
-->
<resultMap id="empResultMap" type="Emp">
    <id property="empId" column="emp_id"/>
    <result property="empName" column="emp_name"/>
    <result property="age" column="age"/>
    <result property="sex" column="sex"/>
    <result property="email" column="email"/>
</resultMap>
<select id="getAllEmp3" resultMap="empResultMap">
    select * from t_emp
</select>
```

### 7.2. 多对一 映射

实体类:

```java
public class Emp {
    private Integer empId;
    private String empName;
    private Integer age;
    private String sex;
    private String email;

    private Dept dept;
}
```

#### 7.2.1. 方式 1: 级联属性赋值

```java
Emp getEmpAndDept(@Param("empId") Integer empId);
```

```xml
<resultMap id="empAndDeptOneResultMap" type="Emp">
    <id property="empId" column="emp_id"/>
    <result property="empName" column="emp_name"/>
    <result property="age" column="age"/>
    <result property="sex" column="sex"/>
    <result property="email" column="email"/>

    <result property="dept.deptId" column="dept_id"/>
    <result property="dept.deptName" column="dept_name"/>
</resultMap>
<select id="getEmpAndDept" resultMap="empAndDeptOneResultMap">
    select * from t_emp
    left join t_dept on t_emp.dept_id = t_dept.dept_id
    where t_emp.emp_id = #{empId}
</select>
```

#### 7.2.2. 方式 2: association

```java
Emp getEmpAndDept2(@Param("empId") Integer empId);
```

```xml
<resultMap id="empAndDeptTwoResultMap" type="Emp">
    <id property="empId" column="emp_id"/>
    <result property="empName" column="emp_name"/>
    <result property="age" column="age"/>
    <result property="sex" column="sex"/>
    <result property="email" column="email"/>
    <!--
    association: 映射 对象类型 的属性
        属性:
            property: 属性名
            javaType: 属性的类型
    -->
    <association property="dept" javaType="Dept">
        <id property="deptId" column="dept_id"/>
        <result property="deptName" column="dept_name"/>
    </association>
</resultMap>
<select id="getEmpAndDept2" resultMap="empAndDeptTwoResultMap">
    select * from t_emp
    left join t_dept on t_emp.dept_id = t_dept.dept_id
    where t_emp.emp_id = #{empId}
</select>
```

#### 7.2.3. 方式 3: 分步查询

先查 emp，拿到 dept_id; 再根据 dept_id 查询 dept

员工:

```java
public interface EmpMapper {
    Emp getEmpAndDept3(@Param("empId") Integer empId);
}
```

```xml
<resultMap id="empAndDeptThreeResultMap" type="Emp">
    <id property="empId" column="emp_id"/>
    <result property="empName" column="emp_name"/>
    <result property="age" column="age"/>
    <result property="sex" column="sex"/>
    <result property="email" column="email"/>

    <!--
    association: 映射 对象类型 的属性
        属性:
            property: 属性名
            select: 关联的查询
            column: 字段名，该字段的值会作为参数传入 select
    -->
    <association
            property="dept"
            select="org.example.mapper.DeptMapper.getDeptById"
            column="dept_id"
    />
</resultMap>
<select id="getEmpAndDept3" resultMap="empAndDeptThreeResultMap">
    select * from t_emp where emp_id = #{empId}
</select>
```

部门:

```java
public interface DeptMapper {
    Dept getDeptById(@Param("deptId") Integer deptId);
}
```

```xml
<select id="getDeptById" resultType="Dept">
    select * from t_dept where dept_id = #{deptId}
</select>
```

#### 7.2.4. 延迟加载

分步查询，可以开启延迟加载

mybatis-config.xml:

```xml
<!--
    全局配置
-->
<settings>
    <!-- 开启全局延迟加载后，所有的分步查询都会延迟加载 -->
    <setting name="lazyLoadingEnabled" value="true"/>
</settings>
```

测试:

```java
@Test
public void testGetEmpAndDept3() {
    EmpMapper empMapper = sqlSession.getMapper(EmpMapper.class);
    Emp emp = empMapper.getEmpAndDept3(3);

    System.out.println(emp.getEmpName());

    // 开启延迟加载后，如果不访问 dept 属性，则不会执行关联的查询
    // System.out.println(emp.getDept());
}
```

开启全局延迟加载后，也可以在具体的语句中通过 fetchType 关闭:

```xml
<!-- 
    fetchType: 默认是 lazy，表示延迟加载，可选值：lazy | eager
-->
<association
        property="dept"
        select="org.example.mapper.DeptMapper.getDeptById"
        column="dept_id"
        fetchType="eager"
/>
```

### 7.3. 一对多 映射

#### 7.3.1. 方式 1: collection

```java
public class Dept {
    private Integer deptId;
    private String deptName;
    private List<Emp> empList;
}
```

```java
Dept getDeptAndEmpById(@Param("deptId") Integer deptId);
```

```xml
<resultMap id="deptAndEmpResultMap" type="Dept">
    <id property="deptId" column="dept_id"/>
    <result property="deptName" column="dept_name"/>

    <!--
        collection: 集合
            属性:
                property: 集合类型的属性的名称
                ofType: 集合属性中元素类型
    -->
    <collection property="empList" ofType="Emp">
        <id property="empId" column="emp_id"/>
        <result property="empName" column="emp_name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <result property="email" column="email"/>
    </collection>
</resultMap>
<select id="getDeptAndEmpById" resultMap="deptAndEmpResultMap" >
    select * from t_dept
    left join t_emp on t_dept.dept_id = t_emp.dept_id
    where t_dept.dept_id = #{deptId}
</select>
```

#### 7.3.2. 方式 2: 分步查询

```java
List<Emp> getEmpByDeptId(@Param("deptId") Integer deptId);
```

```xml
<resultMap id="deptAndEmpResultMap2" type="Dept">
    <id property="deptId" column="dept_id"/>
    <result property="deptName" column="dept_name"/>

    <collection
            property="empList"
            select="org.example.mapper.EmpMapper.getEmpByDeptId"
            column="dept_id"
    />
</resultMap>
<select id="getDeptAndEmpById2" resultMap="deptAndEmpResultMap2" >
    select * from t_dept where dept_id = #{deptId}
</select>
```

## 8. 动态 SQL

拼接 SQL

### 8.1. if

`1=1` 这种恒成立的条件的作用:

1. 不会影响语句的结果
2. 方便拼接 SQL

```java
List<Emp> getEmpByCondition(Emp emp);
```

```xml
<select id="getEmpByCondition" resultType="Emp">
    select * from t_emp where 1=1
    <if test="empName != null and empName != ''">
        and emp_name = #{empName}
    </if>
    <if test="age != null and age != ''">
        and age = #{age}
    </if>
</select>
```

### 8.2. where

>比 if 更进一步

当 where 标签体中 有内容 时:

* 自动生成 where 关键字
* 将标签体内容开头多余的 and 或 or 去掉 (不需要拼接 1=1 这种恒成立的条件了)

当 where 标签中 无内容 时:

* 不会生成 where 关键字

注意: 

* 无法将标签体内容结尾的 and 或 or 去掉
* 也就是说拼接多条件时，必须在开头加 and 或 or

```xml
<select id="getEmpByCondition" resultType="Emp">
    select * from t_emp
    <where>
        <if test="empName != null and empName != ''">
            and emp_name = #{empName}
        </if>
        <if test="age != null and age != ''">
            and age = #{age}
        </if>
    </where>
</select>
```

### 8.3. 操作符

`test` 属性中的表达式的操作符

```sql
// 不等于 null
name != null

// 不等于 空字符串
name != ''

// 等于 18
age == 18

// 与
num > 1 and num < 10

// 或
name != null and name != ''
```

### 8.4. trim

>比 where 更进一步

属性:

* prefix: 添加前缀
* suffix: 添加后缀
* prefixOverrides: 去掉前缀，多个关键词用 “|” 分隔
* suffixOverrides: 去掉后缀，多个关键词用 “|” 分隔

当 trim 标签体中 无内容 时:

* 不会添加 前缀或后缀

```xml
<select id="getEmpByCondition" resultType="Emp">
    select * from t_emp
    <trim prefix="where" prefixOverrides="and|or" suffixOverrides="and|or">
        <if test="empName != null and empName != ''">
            and emp_name = #{empName}
        </if>
        <if test="age != null and age != ''">
            and age = #{age} or
        </if>
    </trim>
</select>
```

### 8.5. choose/when/otherwise

> 类比: if / else if / else

注意:

* when 至少要有一个
* otherwise 最多一个，可以没有

```java
List<Emp> getEmpByChoose(@Param("age") Integer age);
```

```xml
<select id="getEmpByChoose" resultType="Emp" >
    select * from t_emp
    <where>
        <choose>
            <when test="age == 1">
                and 1 = 1
            </when>
            <when test="age == 2">
                and 2 = 2
            </when>
            <otherwise>
                and 3 = 3
            </otherwise>
        </choose>
    </where>
</select>
```

### 8.6. foreach

`<foreach>` 的属性:

* collection: 要遍历的数组或集合
* item: 元素
* separator: 循环体之间的分隔符
* open: 全部内容的前缀
* close: 全部内容的后缀

#### 8.6.1. 遍历数组

```java
int deleteBatch(@Param("empIds") Integer[] empIds);
```

通过 `empIds` / `arg0` /  `array` 访问数组

```xml
<!--
delete from t_emp where emp_id in ( ? , ? , ? )
-->
<delete id="deleteBatch1">
    delete from t_emp where emp_id in
    <foreach collection="empIds" item="empId" open="(" separator="," close=")">
        #{empId}
    </foreach>
</delete>

<!--
delete from t_emp where emp_id = ? or emp_id = ? or emp_id = ?
-->
<delete id="deleteBatch">
    delete from t_emp where
    <foreach item="empId" collection="empIds" separator="or">
        emp_id = #{empId}
    </foreach>
</delete>
```

#### 8.6.2. 遍历集合

```java
int insertBatch(@Param("empList") List<Emp> empList);
```

通过 `empList` / `arg0` /  `list` / `collection` 访问集合

```xml
<!-- 
    insert into t_emp(emp_name, age, sex, email) values 
    (?, ?, ?, ?) , (?, ?, ?, ?) , (?, ?, ?, ?) 
-->
<insert id="insertBatch">
    insert into t_emp(emp_name, age, sex, email) values
    <foreach item="emp" collection="empList" separator=",">
        (#{emp.empName}, #{emp.age}, #{emp.sex}, #{emp.email})
    </foreach>
</insert>
```

### 8.7. SQL 片段

通过 id 引入部分 SQL

```xml
<sql id="empColumns">
    emp_id,
    emp_name,
    age,
    sex,
    email
</sql>
<select id="getAllEmp4" resultType="Emp">
    select <include refid="empColumns"/>
    from t_emp
</select>
```

参考: docs\back-end\java\libs\mybatis\01.MyBatis-常用代码.md 中的 【include 标签】

## 9. 缓存

### 9.1. 一级缓存

一级缓存是 SqlSession 级别的，通过同一个 SqlSession 查询的数据会被缓存，
下次查询相同的数据，就会从缓存中直接获取，不会从数据库重新访问

使一级缓存失效的四种情况：

1. 不同的 SqlSession 对应不同的一级缓存
2. 同一个 SqlSession 但是查询条件不同
3. 同一个 SqlSession 两次查询期间执行了任何一次增删改操作
4. 同一个 SqlSession 两次查询期间手动清空了缓存 (`sqlSession.clearCache()`)

### 9.2. 二级缓存

二级缓存是 SqlSessionFactory 级别，通过同一个 SqlSessionFactory 创建的 SqlSession 查询的结果会被缓存；
此后若再次执行相同的查询语句，结果就会从缓存中获取

二级缓存开启的条件：（必须同时满足）

1. 在核心配置文件中，设置全局配置属性 `cacheEnabled="true"`，默认为 true，不需要设置
2. 在映射文件中设置标签 `<cache />`
3. 二级缓存必须在 SqlSession 关闭或提交之后有效
4. 查询的数据所转换的实体类类型必须实现序列化的接口

使二级缓存失效的情况:

* 两次查询之间执行了任意的增删改，会使一级和二级缓存同时失效

### 9.3. 二级缓存的相关配置

在 mapper 配置文件中添加的 `<cache />` 标签可以设置一些属性

eviction 属性：缓存回收策略

* LRU（Least Recently Used） – 最近最少使用的：移除最长时间不被使用的对象。【默认】
* FIFO（First in First out） – 先进先出：按对象进入缓存的顺序来移除它们。
* SOFT – 软引用：移除基于垃圾回收器状态和软引用规则的对象。
* WEAK – 弱引用：更积极地移除基于垃圾收集器状态和弱引用规则的对象。

flushInterval 属性：刷新间隔，单位毫秒

* 默认情况是不设置，也就是没有刷新间隔，缓存仅仅调用语句时刷新

size 属性：引用数目，正整数

* 代表缓存最多可以存储多少个对象，太大容易导致内存溢出

readOnly 属性：只读，true/false

* true：只读缓存；会给所有调用者返回缓存对象的相同实例。因此这些对象不能被修改。这提供了很重要的性能优势。
* false：读写缓存；会返回缓存对象的拷贝（通过序列化）。这会慢一些，但是安全，因此默认是 false。

### 9.4. 缓存查询的顺序

先查询二级缓存，因为二级缓存中可能会有其他程序已经查出来的数据，可以拿来直接使用。

如果二级缓存没有命中，再查询一级缓存

如果一级缓存也没有命中，则查询数据库

SqlSession 关闭之后，一级缓存中的数据会写入二级缓存

### 9.5. 整合第三方缓存 EHCache

**依赖**:

```xml
<!-- Mybatis EHCache整合包 -->
<dependency>
    <groupId>org.mybatis.caches</groupId>
    <artifactId>mybatis-ehcache</artifactId>
    <version>1.2.1</version>
</dependency>
<!-- slf4j日志门面的一个具体实现 -->
<dependency>
    <groupId>ch.qos.logback</groupId>
    <artifactId>logback-classic</artifactId>
    <version>1.2.3</version>
</dependency>
```

**各 jar 包功能**:

* mybatis-ehcache: Mybatis 和 EHCache 的整合包
* ehcache: EHCache核心包
* slf4j-api: SLF4J 日志门面包
* logback-classic: 支持 SLF4J 门面接口的一个具体实现

**创建 EHCache 的配置文件 ehcache.xml**:

```xml
<?xml version="1.0" encoding="utf-8" ?>
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../config/ehcache.xsd">
    <!-- 磁盘保存路径 -->
    <diskStore path="D:\atguigu\ehcache"/>

    <defaultCache
        maxElementsInMemory="1000"
        maxElementsOnDisk="10000000"
        eternal="false"
        overflowToDisk="true"
        timeToIdleSeconds="120"
        timeToLiveSeconds="120"
        diskExpiryThreadIntervalSeconds="120"
        memoryStoreEvictionPolicy="LRU">
    </defaultCache>

</ehcache>
```

**设置二级缓存的类型**:

```xml
<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>
```

**加入 logback 日志**:

存在 SLF4J 时，作为简易日志的 log4j 将失效，此时我们需要借助 SLF4J 的具体实现 logback 来打印日志。

创建 logback 的配置文件 logback.xml:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true">
    <!-- 指定日志输出的位置 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- 日志输出的格式 -->
            <!-- 按照顺序分别是：时间、日志级别、线程名称、打印日志的类、日志主体内容、换行 -->
            <pattern>[%d{HH:mm:ss.SSS}] [%-5level] [%thread] [%logger] [%msg]%n</pattern>
        </encoder>
    </appender>

    <!-- 设置全局日志级别。日志级别按顺序分别是：DEBUG、INFO、WARN、ERROR -->
    <!-- 指定任何一个日志级别都只打印当前级别和后面级别的日志。 -->
    <root level="DEBUG">
        <!-- 指定打印日志的appender，这里通过“STDOUT”引用了前面配置的appender -->
        <appender-ref ref="STDOUT" />
    </root>
    
    <!-- 根据特殊需求指定局部日志级别 -->
    <logger name="com.atguigu.crowd.mapper" level="DEBUG"/>
</configuration>
```

**EHCache配置文件说明**:

* 略

## 10. MyBatis 逆向工程

正向工程：

* 先创建 Java 实体类，由框架负责根据实体类生成数据库表。
* 比如: Hibernate 

逆向工程：

* 先创建数据库表，由框架负责根据数据库表，反向生成如下资源：
* Java 实体类
* Mapper 接口
* Mapper 映射文件

## 分页插件