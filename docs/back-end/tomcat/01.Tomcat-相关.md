<!--#region
@author 吴钦飞
@email wuqinfei@qq.com
@create date 2025-10-11 11:13:14
@modify date 2025-10-11 11:17:21
@desc [description]
#endregion-->

# Tomcat - 相关

## 1. maxPostSize

说明:

* 文件上传时，FormData 里包含的（非文件）参数默认不能超过 2MB
* 否则，报错如下

```text
The multi-part request contained parameter data (excluding uploaded files) that exceeded the limit for maxPostSize set on the associated connector
```

解决方案:

* 调整 Tomcat 的 maxPostSize 参数

SpringBoot 内置 Tomcat 设置如下:

```yml
server:  
  tomcat:
    # -1 代表不限制
    max-http-post-size: -1
```

参考:

* [parameter data (excluding uploaded files) exceeded the limit for maxPostSize set](https://blog.csdn.net/haponchang/article/details/106049148)